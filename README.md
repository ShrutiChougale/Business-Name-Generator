# Business-Name-Generator

A simple JavaScript-based Business Name Generator that creates random business names by combining predefined words. Built using basic HTML and JavaScript to demonstrate random number generation and conditional logic.

Features

Generates a random business name on each page load

Uses JavaScript Math.random() for randomness

Combines three words to form a business name

Runs entirely in the browser (no backend required)
